#include <iostream> //librerias
#include <conio.h>

 using namespace std;
//Tipos de datos
int main(){
    //estructura variable: tipoDato nombreVariable(custom)
    //numericos, texto, arreglos (listas fijas).
    //numericos:
    int entero; // -> integer -> entero
    short Sentero; // small integer -> entero pequeño
    long Lentero; // large integer -> entero grande
    float flotante; // decimal pequeño
    double Ldec;// decimal grande
    //texto:
    char caracter; //character -> caracter.
    string cadena; //cadena de caracteres.
    //arreglos (listas fijas).
    //tipoDato nombreArreglo [N];
    int listaEnteros [10];
    string listaStrings [10];

    getch();
    return 0;
}